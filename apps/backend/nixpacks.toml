[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'cd ../..',
  'npm install',
  'npm run build:packages',
  'cd apps/backend',
  'npm install'
]

[phases.build]
cmds = [
  'npm run build'
]

[start]
cmd = 'node dist/main.js'
