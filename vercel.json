{
  "version": 2,
  "buildCommand": "turbo run build --filter=@riviso/web",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
